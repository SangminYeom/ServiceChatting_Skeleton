# Chatwoot Webhook 수신 처리 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Chatwoot Webhook 이벤트를 수신하여 DB의 `consultation_logs`를 최신 상태로 유지하고 CSAT 데이터를 수집한다.

**Architecture:** `POST /api/webhook/chatwoot` 단일 엔드포인트에서 HMAC 서명 검증 후 이벤트 유형(`conversation_created`, `conversation_resolved`, `csat_created`)별로 DB를 업데이트한다. `message_created`는 200 OK만 반환하고 무처리한다.

**Tech Stack:** Next.js 16 App Router, TypeScript, Neon.tech PostgreSQL (@neondatabase/serverless), Chatwoot REST Webhook

**Spec:** `docs/superpowers/specs/2026-04-17-webhook-design.md`

---

## 파일 구조

```
scripts/
  migrate-webhook.ts          — csat_rating, csat_comment 컬럼 추가
src/
  lib/
    db.ts                     — updateConsultationStatus(), saveCSAT() 추가
  app/
    api/webhook/chatwoot/
      route.ts                — 신규: 수신, 서명 검증, 이벤트 분기
```

---

## Task 1: DB 마이그레이션 — csat 컬럼 추가

**Files:**
- Create: `scripts/migrate-webhook.ts`

> 참고: `consultation_logs` 테이블에 `resolved_at`은 이미 존재함. `csat_rating`, `csat_comment`만 추가.

- [ ] **Step 1: `scripts/migrate-webhook.ts` 파일 생성**

```ts
import { neon } from "@neondatabase/serverless";

async function migrate() {
  const sql = neon(process.env.DATABASE_URL!);

  await sql`
    ALTER TABLE consultation_logs
      ADD COLUMN IF NOT EXISTS csat_rating INTEGER,
      ADD COLUMN IF NOT EXISTS csat_comment TEXT
  `;

  console.log("Webhook migration complete");
}

migrate().catch(console.error);
```

- [ ] **Step 2: 마이그레이션 실행**

```bash
NODE_TLS_REJECT_UNAUTHORIZED=0 node --env-file=.env.local ./node_modules/tsx/dist/cli.mjs scripts/migrate-webhook.ts
```

Expected: `Webhook migration complete`

- [ ] **Step 3: 커밋**

```bash
git add scripts/migrate-webhook.ts
git commit -m "csat_rating, csat_comment 컬럼 추가 마이그레이션"
```

---

## Task 2: db.ts에 updateConsultationStatus, saveCSAT 추가

**Files:**
- Modify: `src/lib/db.ts`

- [ ] **Step 1: `src/lib/db.ts` 끝에 두 함수 추가**

```ts
export async function updateConsultationStatus(
  chatwootConversationId: number,
  status: string,
  resolvedAt?: Date
): Promise<void> {
  const sql = getDb();
  if (resolvedAt) {
    await sql`
      UPDATE consultation_logs
      SET status = ${status}, resolved_at = ${resolvedAt.toISOString()}
      WHERE chatwoot_conversation_id = ${chatwootConversationId}
    `;
  } else {
    await sql`
      UPDATE consultation_logs
      SET status = ${status}
      WHERE chatwoot_conversation_id = ${chatwootConversationId}
    `;
  }
}

export async function saveCSAT(
  chatwootConversationId: number,
  rating: number,
  comment: string | null
): Promise<void> {
  const sql = getDb();
  await sql`
    UPDATE consultation_logs
    SET csat_rating = ${rating}, csat_comment = ${comment}
    WHERE chatwoot_conversation_id = ${chatwootConversationId}
  `;
}
```

- [ ] **Step 2: TypeScript 타입 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add src/lib/db.ts
git commit -m "DB에 updateConsultationStatus, saveCSAT 함수 추가"
```

---

## Task 3: Webhook API Route 구현

**Files:**
- Create: `src/app/api/webhook/chatwoot/route.ts`

- [ ] **Step 1: `src/app/api/webhook/chatwoot/route.ts` 파일 생성**

```ts
import { NextRequest, NextResponse } from "next/server";
import { createHmac } from "crypto";
import { updateConsultationStatus, saveCSAT } from "@/lib/db";

function verifySignature(body: string, signature: string): boolean {
  const secret = process.env.CHATWOOT_WEBHOOK_SECRET!;
  const expected = createHmac("sha256", secret).update(body).digest("hex");
  return expected === signature;
}

function csatRatingToNumber(rating: string): number {
  if (rating === "satisfied") return 5;
  if (rating === "neutral") return 3;
  return 1;
}

export async function POST(request: NextRequest) {
  const body = await request.text();
  const signature = request.headers.get("x-chatwoot-hmac-sha256") ?? "";

  if (!verifySignature(body, signature)) {
    return NextResponse.json({ error: "Invalid signature" }, { status: 401 });
  }

  const payload = JSON.parse(body) as Record<string, unknown>;
  const event = payload.event as string;

  try {
    if (event === "conversation_created") {
      const conversationId = payload.id as number;
      await updateConsultationStatus(conversationId, "active");
    } else if (event === "conversation_resolved") {
      const conversationId = payload.id as number;
      await updateConsultationStatus(conversationId, "resolved", new Date());
    } else if (event === "csat_created") {
      const conversation = payload.conversation as { id: number };
      const rating = csatRatingToNumber(payload.rating as string);
      const comment = (payload.feedback_message as string | undefined) ?? null;
      await saveCSAT(conversation.id, rating, comment);
    }
    // message_created: 무처리
  } catch (error) {
    console.error("Webhook 처리 실패:", error);
    return NextResponse.json({ error: "Internal server error" }, { status: 500 });
  }

  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 2: TypeScript 타입 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add src/app/api/webhook/chatwoot/route.ts
git commit -m "Chatwoot Webhook 수신 API Route 구현"
```

---

## Task 4: 환경 변수 설정 + Chatwoot Webhook 등록 + 배포 + E2E 테스트

- [ ] **Step 1: `.env.local`에 환경 변수 추가**

`.env.local` 파일에 추가:

```
CHATWOOT_WEBHOOK_SECRET=<Chatwoot Webhook 설정에서 생성한 시크릿>
```

- [ ] **Step 2: Chatwoot Webhook URL 등록**

Chatwoot 대시보드 → Settings → Integrations → Webhooks → New Webhook:
- **URL:** `https://service-chatting.vercel.app/api/webhook/chatwoot`
- **Subscriptions:** `conversation_created`, `conversation_resolved`, `message_created`, `csat_survey_submitted`
- **Secret:** 임의의 문자열 입력 (이 값을 `CHATWOOT_WEBHOOK_SECRET`에 사용)

- [ ] **Step 3: Vercel 환경 변수 추가**

Vercel 대시보드 → service-chatting → Settings → Environment Variables:
- `CHATWOOT_WEBHOOK_SECRET` = Step 1에서 설정한 값

- [ ] **Step 4: push 및 배포**

```bash
git push
```

Expected: GitHub Actions가 Vercel Production 배포 트리거.

- [ ] **Step 5: E2E 테스트**

1. https://service-chatting.vercel.app 에서 상담사 연결 → 대화 생성
2. Chatwoot 대시보드에서 해당 대화 Resolve
3. Neon.tech 콘솔에서 확인:
   ```sql
   SELECT status, resolved_at FROM consultation_logs ORDER BY id DESC LIMIT 1;
   ```
   Expected: `status = 'resolved'`, `resolved_at` 값 있음
